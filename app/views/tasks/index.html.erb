<ul>
<% @tasks.each do |task| %>
  <li><%= "Status: #{task.completed} - " %> <%= link_to task.title, task_path(task) %> <%= link_to "Delete",
  task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></li>
<% end %>
</ul>

<%= link_to 'Add new task', 'tasks/new' %>
