<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<% if @task.completed %>
  <p>[X] The task is completed.</p>
<% else %>
  <p>[ ] The task is not completed.</p>
<% end %>

<%= link_to 'Back to list', '/tasks' %>
<%= link_to 'Edit task', "/tasks/#{params[:id]}/edit" %>
